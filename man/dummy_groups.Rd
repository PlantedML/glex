% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group_components.R
\name{dummy_groups}
\alias{dummy_groups}
\title{Derive feature groups from the factors behind a dummy encoding}
\usage{
dummy_groups(
  object,
  data,
  naming = function(feature, levels) paste0(feature, levels)
)
}
\arguments{
\item{object}{(\code{glex}) Object of class \code{glex}.}

\item{data}{(\code{data.frame}) The data before dummy encoding; its factor
columns define the candidate groups. Non-factor columns are ignored.}

\item{naming}{(\verb{function(feature, levels)}) Maps a factor name and its
levels to the encoded column names. Defaults to the \code{\link[=model.matrix]{model.matrix()}}
convention \code{paste0(feature, levels)}.}
}
\value{
Named list of encoded column names, one element per matched
factor, suitable as the \code{groups} argument of \code{\link[=group_components]{group_components()}}.
}
\description{
Builds the \code{groups} list for \code{\link[=group_components]{group_components()}} from the original,
un-encoded data: every factor column of \code{data} whose encoded level columns
are found in \code{object$x} becomes one group. By default, level columns are
expected under the \code{\link[=model.matrix]{model.matrix()}} naming convention
(\code{paste0(feature, levels)}, e.g. \code{season} with level \code{"Winter"} becomes
\code{"seasonWinter"}), which covers both one-hot (\code{~ f - 1}) and treatment
(\code{~ f}) coding -- levels without a matching column (such as a dropped
reference level) are simply skipped.
}
\details{
If the data was encoded with a different scheme, pass \code{naming}: a function
of \verb{(feature, levels)} returning the encoded column names, e.g.
\code{function(feature, levels) paste(feature, levels, sep = "_")}. It must
reproduce the column names the model was trained with, i.e. those found in
\code{object$x}.
}
\examples{
\dontshow{if (requireNamespace("xgboost", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(xgboost)
set.seed(1)
n <- 200
d <- data.frame(
  f = factor(sample(c("a", "b", "c"), n, replace = TRUE)),
  x1 = rnorm(n)
)
y <- c(a = 0, b = 2, c = -1)[d$f] + d$x1 + rnorm(n)
x <- model.matrix(~ f + x1 - 1, d)

xg <- xgboost(x, y, nrounds = 10, max_depth = 3, nthreads = 1)
gl <- glex(xg, x)

dummy_groups(gl, d)
grouped <- group_components(gl, dummy_groups(gl, d))
names(grouped$m)
\dontshow{\}) # examplesIf}
}
