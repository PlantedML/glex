% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group_components.R
\name{group_components}
\alias{group_components}
\title{Group features of a decomposition}
\usage{
group_components(object, groups)
}
\arguments{
\item{object}{(\code{glex}) Object of class \code{glex}.}

\item{groups}{(\code{list}) Named list of character vectors of feature names in
\code{object$x}. Each vector becomes a single feature named after the list
element, e.g. \code{list(f = c("fa", "fb", "fc"))}. Features may appear in at
most one group; features not listed remain unchanged.}
}
\value{
Object of class \code{glex} with grouped \code{m}, \code{shap} and \code{x}.
}
\description{
Aggregates the terms of a decomposition such that a set of features is
treated as a single feature, named after the group. All terms involving only
members of a group are summed into the group's main effect, and terms mixing
group members with other features become interactions of the group. Since the
decomposition is additive, this regrouping is exact: components still sum to
the model prediction, and SHAP values of a group are the sums of its members'
SHAP values, preserving the efficiency property.
}
\details{
The motivating use case is dummy-encoded categorical features in \code{xgboost}:
the decomposition of a one-hot encoded factor spreads its effect across the
dummies and their interactions (e.g. \code{fa:fb}), which are artifacts of the
encoding rather than meaningful interactions. Grouping the dummies restores
the factor as one feature, e.g. \code{groups = list(f = c("fa", "fb", "fc"))}
turns \code{fa}, \code{fb}, \code{fc}, \code{fa:fb}, ... into \code{f}, and \code{fa:x1}, \code{fa:fb:x1}, ...
into \code{f:x1}.

For the grouped columns of \code{x}, a factor is reconstructed when the group's
columns form a dummy encoding (0/1 values, at most one active per row), with
the active column's name as the level and \code{"(base)"} for rows with no active
column (treatment coding). Otherwise the group's column in \code{x} is \code{NA} and
plot functions cannot be used for that group's main effect.
}
\examples{
\dontshow{if (requireNamespace("xgboost", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(xgboost)
set.seed(1)
n <- 200
f <- factor(sample(c("a", "b", "c"), n, replace = TRUE))
x1 <- rnorm(n)
y <- c(a = 0, b = 2, c = -1)[f] + x1 + c(a = 1, b = 0, c = -1)[f] * x1 + rnorm(n)
x <- cbind(model.matrix(~ f - 1), x1)

xg <- xgboost(x, y, nrounds = 10, max_depth = 3, nthreads = 1)
gl <- glex(xg, x)
names(gl$m)

grouped <- group_components(gl, groups = list(f = c("fa", "fb", "fc")))
names(grouped$m)
all.equal(rowSums(grouped$m), rowSums(gl$m))
\dontshow{\}) # examplesIf}
}
