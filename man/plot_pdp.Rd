% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_main_effect.R
\name{plot_pdp}
\alias{plot_pdp}
\title{Partial Dependence Plot}
\usage{
plot_pdp(object, predictor, rug_sides = "b", ...)
}
\arguments{
\item{object}{Object of class \code{\link{glex}}.}

\item{predictor}{\code{(character(1))} predictor names, e.g. \code{"x1"} to plot
main effect of \code{x1}.}

\item{rug_sides}{\code{(character(1): "b")} Sides to plot rug (see \code{\link[ggplot2:geom_rug]{ggplot2::geom_rug()}}) plot on for continuous predictors..
Default is \code{"b"} for both sides. Set to \code{"none"} to disable rug plot.}

\item{...}{Used for future expansion.}
}
\value{
A \code{ggplot2} object.
}
\description{
A version of \code{\link{plot_main_effect}} with the intercept term (horizontal line) added,
resulting in a partial dependence plot.
}
\examples{
if (requireNamespace("randomPlantedForest", quietly = TRUE)) {
library(randomPlantedForest)

# introduce factor variables to show categorical feature handling
mtcars$cyl <- factor(mtcars$cyl)
mtcars$vs <- factor(mtcars$vs)

# Fit forest, get components
set.seed(12)
rpfit <- rpf(mpg ~ cyl + wt + hp + drat + vs, data = mtcars, ntrees = 25, max_interaction = 3)
components <- glex(rpfit, mtcars)

plot_pdp(components, "wt")
plot_pdp(components, "cyl")
}
}
\seealso{
\code{\link[=plot_main_effect]{plot_main_effect()}}

Other Visualization functions: 
\code{\link{autoplot.glex}()},
\code{\link{autoplot.glex_vi}()},
\code{\link{glex_explain}()}
}
\concept{Visualization functions}
